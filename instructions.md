# Global Coding Agent Instructions

- Default response language: Japanese
- Always respond in Japanese unless explicitly asked to use another language
- Do not create git commits unless explicitly instructed to do so
- Do not run git push unless explicitly instructed to do so
- Only commit or push when the user explicitly says "commit" or "push"
